git init   //*запуск гита
git status   //*статус
git add *имя файла*   //*добавить файлы
git commit -m "сюда добавить комментарий"   //*перенести в рабочую область
git log //*история изменений - выход через q
git remote -v   //*посмотреть доступные удаленные адреса
git push origin master
